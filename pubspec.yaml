name: americonictv_mobile
description: AmericonicTV mobile streaming platform

publish_to: 'none' 

version: 1.2.2+2

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.2
  intl: ^0.16.1
  shared_preferences: ^2.0.3
  video_player: ^2.1.1
  wakelock: ^0.5.1
  webview_flutter: ^2.0.4
  flutter_linkify: ^5.0.2
  url_launcher: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/images/categories/
